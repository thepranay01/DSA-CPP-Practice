#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cout << "n: ";
    cin >> n;
    cout << "n: ";
    cin >> m;

    vector<vector<int>> guard[][];
    while(g)
    for(int i = 0; i<n;)
    vector<vector<int>> walls;

    return 0;
}