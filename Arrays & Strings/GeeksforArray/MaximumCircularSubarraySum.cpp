#include <bits/stdc++.h>
using namespace std;
int MaxCirSubSum(vector<int> arr, int n)
{
    
}

int main()
{
    int n;
    cin >> n;

    vector<int> nums(n);
    for (int i = 0; i < n; i++)
    {
        cin >> nums[i];
    }

    cout << MaxCirSubSum(arr, n);

    return 0;
}